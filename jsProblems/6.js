// find the output

const arr =[1,2,3]
arr[50] =[50]
console.log(arr.length);
// output is 51

// when setting a property on javascript array
// when the property is a valid array index and that
// index is outside the current bounds of the arra,
// the engine will update the array's length aacordingly.
