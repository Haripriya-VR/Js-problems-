// 8. find the last element of an array(without using the length property)
const arr =['a','b','c','d']

// console.log(arr.pop());
//  pop will remove the last item from an array and returns it.

console.log(arr.at(-1));

// The at() method takes an integer value and gives the item at that index in the array.
// if negative value is given it will count back from the last item inn the array.


